const API_ERROR = require('./api-error');
const CONVERSION_ERROR = require('./conversion-error');
const INVALID_CURRENCY = require('./invalid-currency');

module.exports = {
  API_ERROR,
  CONVERSION_ERROR,
  INVALID_CURRENCY
};
